[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "prostate-fluence"
version = "0.1.0"
description = "FluenceFormer experiments (ablation + prediction saving)"
requires-python = ">=3.9"